{
  "buildCommand": "cd client && npm install && npm run build-only",
  "outputDirectory": "client/dist",
  "installCommand": "echo 'Dependencies installed in buildCommand'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}