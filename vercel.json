{
  "buildCommand": "npm install --prefix client && npm run build-only --prefix client",
  "outputDirectory": "client/dist",
  "installCommand": "echo 'Dependencies installed in buildCommand'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}